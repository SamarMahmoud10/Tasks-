import 'package:flutter/material.dart';

/// Color palette extracted directly from the Figma design spec.
class AppColors {
  AppColors._();

  static const Color white = Color(0xFFFFFFFF);
  static const Color green = Color(0xFF168C4B);
  static const Color black = Color(0xFF181818);

  /// Outgoing (sent) message bubble color.
  static const Color outgoingBubble = Color(0xFF232D3D);

  /// Slightly lighter tone used for the doodle pattern on the dark background.
  static const Color doodle = Color(0x14FFFFFF);
}
